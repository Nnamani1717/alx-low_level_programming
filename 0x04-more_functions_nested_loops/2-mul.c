#include "main.h"
/**
 * mul - multiplies two integers
 * @x: the first numbr to multiply
 * @y: the second nuber to mulitply
 * Return: the result of the mulitplication between x param and y param
 */
int mul(int x, int y)
{
	return (x * y);
}
